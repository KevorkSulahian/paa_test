from setuptools import setup, find_packages

setup(
    name='paa_test',  # Name of your package
    version='0.1',
    packages=find_packages(),
    install_requires=[],  # Add any dependencies here if needed
)
